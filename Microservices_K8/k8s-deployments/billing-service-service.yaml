# billing-service-service.yaml
apiVersion: v1
kind: Service
metadata:
  name: Billing-ELB
spec:
  selector:
    app: Billing-ELB
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer
